#include "Common.h"
#include <WinSock2.h>

//·Ç×èÈûconnect
void nonblockingConnect()
{
    SOCKET sock;
    SOCKADDR_IN srvAddr;
    memset(&srvAddr, 0, sizeof(SOCKADDR_IN));
    srvAddr.sin_addr = htonl()

    connect(sock, )
}